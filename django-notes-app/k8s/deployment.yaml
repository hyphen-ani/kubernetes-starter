apiVersion: apps/v1
kind: Deployment
metadata:
  name: notes-app-deployement
  namespace: nginx
  labels:
    app: notes-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: notes-app
  template:
    metadata:
      labels:
        app: notes-app
    spec:
      containers:
      - name: notes-app
        image: anirudhhyphen/notes-app
        ports:
        - containerPort: 8000
